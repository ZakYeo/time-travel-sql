# Working map

- Preserve `GOAL.md` and its initial commit. It is the full acceptance contract.
- Read `docs/development-status.md` for current work and actual evidence.
- Follow `docs/architecture.md` and `docs/correctness.md` for canonical rules.
- Run `npm run check` before commits; never bypass hooks. Node is pinned in `.nvmrc`.
- Use personal Git identity Zak <zakyeomanson@gmail.com> and the ZakYeo remote.
- Commit validated slices often; push periodically as authorised by the user.
- Request fresh read-only thermonuclear sub-agent reviews at substantial milestones;
  implement actionable feedback before proceeding.
- Never publish packages or releases. Keep local runtime data under ignored paths.
